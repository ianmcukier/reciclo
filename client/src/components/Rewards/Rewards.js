import React from 'react';
import PropTypes from 'prop-types';

const Rewards = props => {
    return (
        <div>
            premios
        </div>
    );
};

Rewards.propTypes = {
    
};

export default Rewards;